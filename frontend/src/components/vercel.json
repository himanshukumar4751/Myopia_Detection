{
    "installCommand": "pip install -r requirements.txt",
    "outputDirectory": "dist",
    "buildCommand": "python app.py",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}